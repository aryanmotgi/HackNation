# Copy to .env (gitignored) and fill in. Demo falls back to an offline mock if unset.
OPENAI_API_KEY=sk-your-key-here

# Optional model override. Default is gpt-5.6-terra.
# NOTE: the bare 'gpt-5.6' alias routes to Sol, not Terra — keep the -terra suffix.
OPENAI_MODEL=gpt-5.6-terra
