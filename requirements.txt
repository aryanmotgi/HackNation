# Python 3.12 (kuzu has no prebuilt wheel for 3.14 yet).
kuzu==0.11.3          # graph + native vector index (memory layer)
openai==2.46.0        # negotiation agent + customer bot (gpt-5.6-terra)
python-dotenv==1.2.2  # load OPENAI_API_KEY from .env
flask==3.1.3          # manufacturer dashboard server (frontend/)

# Swap the stub embedder for this before the demo (see memory/embeddings.py):
# sentence-transformers==3.0.1
